//TODO: add image when flip to local images instead of remote
html = "<%= escape_javascript(render(:partial => 'phone_details', :locals => {:phone => @phone})) %>";
$(html).appendTo($(".phone-details"));