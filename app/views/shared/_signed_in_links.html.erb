<%
	@links = {
      :admin => [
        { :name  => "Users", :url => admin_users_path, :class => "" },
        { :name  => "Phones", :url => admin_phones_path }
      ],
      :store => [
        { :name  => I18n.t("header.catalogue"), :url => catalogue_path(:username => current_account.username), :icon => "icon-list icon-white" },
        { :name => I18n.t("header.messages"), :url => messages_path, :icon => "icon-envelope icon-white" }
      ],
      :user => [
        { :name  => I18n.t("header.catalogue"), :url => catalogue_path(:username => current_account.username), :icon => "icon-list icon-white" },
        { :name => I18n.t("header.messages"), :url => messages_path, :icon => "icon-envelope icon-white" }
      ]
    }
%>
 
<% 		@links[current_account.rolable.class.name.downcase.to_sym].each do |item| %>
<%=       content_tag(:li) do %>
<%=          link_to item[:url] do %>
				<i class="<%= item[:icon] %>"></i><%= item[:name] %>
<% 			 end %>
<%        end %>
<%      end %>